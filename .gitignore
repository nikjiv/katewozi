.DS_Store
Thumbs.db

node_modules/
.env
.env.local
.env.*.local

.vercel
.cache
*.log

# PNG sources for the hero/about images — only the converted .webp versions are deployed.
# Keep the PNGs on disk locally so they can be re-encoded, but don't ship them.
images/*.png
